public virtual with sharing class LeadController extends SobjectController {
    public LeadController(ApexPages.StandardController controller) {
        super(controller);
    }

    public override virtual PageReference initialize() {
        return super.initialize();
    }
}