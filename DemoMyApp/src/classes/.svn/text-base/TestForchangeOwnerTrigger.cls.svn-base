// test method for changeOwner Trigger on task and changetaskOwner trigger on contact
@IsTest

private class TestForchangeOwnerTrigger {
	/*
    static testMethod void testinsertTask(){ 
    User user = [select id from user limit 1];
        // create new contact
        Contact c = new contact();
        c.lastname = 'test';
        insert c;
        system.assert(c.id!=null);        
        
        // create new task
        Task t= new task();
        t.whoid = c.id;
        insert t;
        system.assert(t.id!=null);        
        
        // Update contact for changeTaskOwner trigger        
        c.ownerid = user.id;
        update c;
        system.assert(c.id!=null);        
    } 
    */
}